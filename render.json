{
  "name": "safestride-banking",
  "env": "node",
  "buildCommand": "npm install && cd frontend && npm install && npm run build && cp -r build ../backend/public",
  "startCommand": "cd backend && npm start",
  "envVars": [
    {
      "key": "NODE_ENV",
      "value": "production"
    },
    {
      "key": "PORT",
      "value": "3001"
    }
  ]
} 